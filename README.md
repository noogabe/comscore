Para executar o projeto:
- Instale o Docker e o Git
- Clone o repositório
- Na raiz do projeto, crie um arquivo chamado `.env`. Copie o conteúdo de env.example e cole dentro dele. 
- Execute o comando:
```
docker compose up --build
```
